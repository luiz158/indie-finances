<%= form_for(@dre) do |f| %>
  <% if @dre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dre.errors.count, "error") %> prohibited this dre from being saved:</h2>

      <ul>
      <% @dre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :adwords %><br />
    <%= f.number_field :adwords %>
  </div>
  <div class="field">
    <%= f.label :facebook_add %><br />
    <%= f.number_field :facebook_add %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
